/**
 * Test case for namingMix.
 * Runs with mocha.
 */
'use strict'

const namingMix = require('../lib/mixins/naming_mix.js')
const assert = require('assert')
const co = require('co')

describe('naming-mix', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Naming mix', () => co(function * () {

  }))
})

/* global describe, before, after, it */
